# Elo Tests

This package runs standalone Elo estimation with deterministic paired games.

Run quick check:

python -m elo_tests.cli --config elo_tests/configs/quick.json

Run default:

python -m elo_tests.cli --config elo_tests/configs/default.json
